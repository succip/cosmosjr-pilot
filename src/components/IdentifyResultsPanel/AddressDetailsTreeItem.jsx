import TreeItem from "@mui/lab/TreeItem";
import { addressDetails } from "../../config/Settings";

const AddressDetailsTreeItem = ({ url, tableHeadings, lookupValue, mslink, propertyNumber }) => {
  return <TreeItem nodeId />;
};

export default AddressDetailsTreeItem;
